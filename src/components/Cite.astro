---
const { refKey } = Astro.props as { refKey: string };
if (!refKey) throw new Error("<Cite /> requer a prop 'refKey'.");
---
<!-- Placeholder que o plugin substituirá por um botão numerado + popover -->
<cite class="oc-cite" data-key={refKey}></cite>
